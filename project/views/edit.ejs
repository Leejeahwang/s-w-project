<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>노트 수정</title>
</head>
<body>
  <h2>노트 수정</h2>
  <form method="POST" action="/notes/<%= note.id %>/edit">
    <p>제목: <input type="text" name="title" value="<%= note.title %>" required /></p>
    <p>내용: <textarea name="summary" placeholder="요약" required><%= note.summary %></textarea></p>
    <p>이수 구분:
      <select name="category">
        <% categories.forEach(c => { %>
          <option value="<%= c.name %>" <%= c.name === note.category ? "selected" : "" %>>
            <%= c.name %>
          </option>
        <% }) %>
      </select>
    </p>
    <p>과목:
      <select name="subject">
        <% subjects.forEach(s => { %>
          <option value="<%= s.name %>" <%= s.name === note.subject ? "selected" : "" %>>
            <%= s.name %>
          </option>
        <% }) %>
      </select>
    </p>
    <p>학년:
      <select name="year">
        <% years.forEach(y => { %>
          <option value="<%= y.name %>" <%= y.name === note.year ? "selected" : "" %>>
            <%= y.name %>
          </option>
        <% }) %>
      </select>
    </p>
    <p>학기:
      <select name="semester">
        <% semesters.forEach(sm => { %>
          <option value="<%= sm.name %>" <%= sm.name === note.semester ? "selected" : "" %>>
            <%= sm.name %>
          </option>
        <% }) %>
      </select>
    </p>
    <input name="professor"  placeholder="교수명"  value="<%= note.professor %>"  />

    <button type="submit">수정</button>
  </form>

  <a href="/notes">목록으로 돌아가기</a>
</body>
</html>
