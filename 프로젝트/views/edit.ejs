<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>노트 수정</title>
</head>
<body>
  <h2>노트 수정</h2>
  <form method="POST" action="/notes/<%= note.id %>/edit">
    <input name="subject"    value="<%= note.subject %>"    required />
    <input name="professor"  value="<%= note.professor %>"  />
    <select name="category" id="note.category">
      <option value="전공필수">전공필수</option>
      <option value="전공선택">전공선택</option>
      <option value="교양">교양</option>
    </select>
    <textarea name="summary" required><%= note.summary %></textarea>
    <button type="submit">저장</button>
  </form>
  <a href="/notes">목록으로 돌아가기</a>
</body>
</html>